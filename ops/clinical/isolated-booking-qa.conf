# Optional, reversible override for end-to-end tests against the isolated DB.
# Keep the base runtime.env and every security/delivery setting unchanged.
[Service]
EnvironmentFile=/etc/clinicaclick-dev/clinical-qa.env
